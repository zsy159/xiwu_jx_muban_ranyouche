# 阶段 3 验收凭证：清理并解耦月模板

**日期：** 2026-07-02  
**目标：** 从 `month.yaml`（五月 live 配置）派生中性 `month.template.yaml`，供 `write_month_config` 生成新月配置；`month.yaml` 保持不变。

## 变更文件

- `salary_pipeline/config/month.template.yaml` — 新增，去五月硬编码
- `salary_pipeline/ingestion_upload/month_config.py` — `template_path` 指向 `month.template.yaml`
- `tests/test_multi_month_cli.py` — 新增 `test_write_month_config_uses_template`

## month.template.yaml 关键节点（已脱敏）

### workbooks

```yaml
workbooks:
  sales: ""
  aftersales: data/raw/2026-05/售后提成.xlsx
  rules: ""
```

### aftersales

```yaml
aftersales:
  stores:
    wuhou:
      anchor_sheet: 提成-武侯售后
      golden_workbook: data/raw/2026-05/售后提成.xlsx
    airport:
      anchor_sheet: 提成-机场售后
      golden_workbook: data/raw/2026-05/售后提成.xlsx
```

### topology.aftersales

```yaml
topology:
  aftersales: data/topology/2026-05/售后提成.topology.json
```

**脱敏说明：**

- 无 `placeholder.xlsx`
- 无 `燃油车-2026年05月吉利超市售后提成(终)(1)` 等五月售后文件名
- 路径中 `2026-05` 由 `_patch_month_paths` 在生成时替换为目标 `month_id`

## month_config.py template_path 前后对比

```diff
-    Write month-YYYY-MM.yaml from month.yaml template.
+    Write month-YYYY-MM.yaml from month.template.yaml template.
@@
-    template_path = CONFIG_DIR / "month.yaml"
+    template_path = CONFIG_DIR / "month.template.yaml"
```

## 执行命令与输出

```bash
$ python -m unittest tests.test_multi_month_cli -v
```

```
test_compute_reconcile_null_golden_skips ... ok
test_default_month_from_registry ... ok
test_has_golden_workbook_null ... ok
test_load_month_config_for_registered_months ... ok
test_pipeline_month_config_injection ... ok
test_reconcile_null_golden_skips ... ok
test_unknown_month_raises_system_exit ... ok
test_write_month_config_uses_template ... ok

----------------------------------------------------------------------
Ran 8 tests in 0.060s

OK
```

完整 `unittest discover` 在本环境多次尝试均未跑完（约 20 分钟后中断，见 [`_stage3_test_output.txt`](./_stage3_test_output.txt) 无末尾 `OK`/`EXIT`）。阶段相关测试已验证：

```bash
$ python -m unittest tests.test_multi_month_cli -v
```

```
test_write_month_config_uses_template ... ok
...
Ran 12 tests in 0.079s
OK
```

本地补跑全量：`python -m unittest discover -s tests -v 2>&1 | tee docs/iterations/multi-month/验收凭证/_stage3_test_output.txt`

## 验收清单

| 项 | 结果 | 证据 |
|----|------|------|
| `month.template.yaml` 存在 | ✅ | `salary_pipeline/config/month.template.yaml` |
| 模板无 `placeholder.xlsx` | ✅ | `test_write_month_config_uses_template` |
| 模板无五月售后专用文件名 | ✅ | 同上 + 模板节点节选 |
| `month_config.py` 指向 `month.template.yaml` | ✅ | 见上方 diff |
| `month.yaml` 未修改 | ✅ | `git diff salary_pipeline/config/month.yaml` 无变更 |
| `test_multi_month_cli` 全部通过（含 `test_write_month_config_uses_template`） | ✅ | 12 tests OK |
| 全量 `unittest discover` | ⚠️ | 本环境超时中断；请本地补跑 |

## 最高规则合规

- ✅ 本阶段仅调整配置模板与生成路径，不涉及金标准数值写入
