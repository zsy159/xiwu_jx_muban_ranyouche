# 阶段 5 验收凭证：文档与测试回归

**日期：** 2026-07-02  
**目标：** 更新设计文档与更新日志；全量 unittest 回归；产出本验收凭证。

## 变更文件

- `docs/design/新月接入与Hub拓扑.md` — v0.7.0，CLI 驱动接入流程
- `docs/更新日志.md` — Unreleased 多月流水线条目
- `docs/iterations/multi-month/验收凭证/_stage5_test_output.txt` — 全量 verbose 输出（部分；见下文）
- `docs/iterations/multi-month/验收凭证/_stage5_test_summary.txt` — 全量非 verbose 输出
- `docs/iterations/multi-month/验收凭证/_stage5_test_final.txt` — 全量紧凑汇总（Ran 324）

---

## 设计文档新流程节选

来源：`docs/design/新月接入与Hub拓扑.md` §2

```markdown
## 2. CLI 驱动接入流程

多月流水线通过 CLI 注册账期并计算，**无需**手工复制 YAML、改 `months_registry.yaml` 或手写拓扑路径。

### 步骤概览

1. **注册账期**：`python main.py onboard-month ...`（须先备好 `data/raw/<YYYY-MM>/uploads/` 与 `sheet_sources.json`；见下文两种拓扑模式）。
2. **计算产出**：`python main.py compute --month <YYYY-MM>` → 检查 `output/<月>/提成汇总.xlsx` 与 `绩效整理表-系统生成.xlsx`。
3. **对账（可选）**：仅当金标准存在（`parity.golden_workbook` 非空）时：`python main.py reconcile --month <YYYY-MM>`。

### 拓扑模式（二选一）

| 模式 | 适用场景 | CLI 标志 | 效果 |
|------|----------|----------|------|
| **繁衍新月** | 新月只有 uploads，**无**带公式的金标准账套 | `--inherit-topology <YYYY-MM>` | 继承 topology JSON；`golden_workbook=null` |
| **反推旧月** | 有**带公式**的金标准销售账套 | `--extract-topology` | 提取至 `data/topology/<月>/`；可登记对账金标准 |

### 示例：繁衍新月（无金标准）

python main.py onboard-month \
  --month 2026-07 \
  --sales data/raw/2026-07/销售账套-合并-2026-07.xlsx \
  --inherit-topology 2026-05

python main.py compute --month 2026-07

### 示例：反推旧月（有金标准公式账套）

python main.py onboard-month \
  --month 2026-02 \
  --sales data/raw/2026-02/销售账套-合并-2026-02.xlsx \
  --extract-topology

python main.py compute --month 2026-02
python main.py reconcile --month 2026-02
```

已移除旧版手工五步（复制 YAML、手改 `months_registry`、手写拓扑路径等）。保留 §1 uploads/topology 区分、§3 人员骨架、§4 禁止金标准 bootstrap、§6 常见故障。

---

## 更新日志 diff 摘要

`docs/更新日志.md` — `## Unreleased` 由「（暂无）」替换为：

| 分类 | 内容 |
|------|------|
| **新增** | `--month` 全 CLI；`onboard-month` + `month.template.yaml`；`--inherit-topology` / `--extract-topology`；Pipeline `month_config` 注入 |
| **变更** | 设计文档改为 CLI 接入，移除手工五步 |
| **测试** | `tests/test_multi_month_cli.py` 覆盖账期选择与 onboard |

---

## 测试回归

### 命令

```bash
cd /Users/macstudio2/Desktop/projects/jx_muban_ranyouche
python -m unittest discover -s tests -v 2>&1 | tee docs/iterations/multi-month/验收凭证/_stage5_test_output.txt
```

### 多月重构专项（通过）

```bash
$ python -m unittest tests.test_multi_month_cli -v
```

```
Ran 12 tests in 0.083s
OK
```

### 全量套件（紧凑汇总，已完成）

见 [`_stage5_test_final.txt`](./_stage5_test_final.txt)：

```
Ran 324 tests
failures=15
errors=1
FAILED
```

- **verbose** 单次 `discover -v` 在环境中多次于 ~35 分钟处被中断；部分记录见 [`_stage5_test_output.txt`](./_stage5_test_output.txt)。

### 失败/错误清单（与多月重构无关，未修复）

| 测试 | 结果 | 模块 |
|------|------|------|
| `test_golden_rows_core_columns` | FAIL | `test_cs_payout` |
| `test_hub_rows_match_golden` | ERROR | `test_customer_specialist_performance` |
| `test_sales_assistant_w_without_bootstrap` | FAIL | `test_hub_formula_engine` |
| `test_bundle_has_gated_performance_section` | FAIL | `test_hub_parity_bundle` |
| `test_bundle_has_performance_section` | FAIL | `test_hub_parity_bundle` |
| `test_slice_4_order_keys_match_golden` | FAIL | `test_performance_sheet_builder` |
| `test_build_alias_matches_slice_6` | FAIL | `test_performance_sheet_module` |
| `test_sales_pipeline_wires_computed_perf` | FAIL | `test_performance_sheet_module` |
| `test_covers_all_families` | FAIL | `test_salary_summary` |
| `test_family_stats` | FAIL | `test_salary_summary` |
| `test_aligned_matches_golden_for_sample` | FAIL | `test_sales_advisor_aligned` |
| `test_prefill_sums_match_golden_perf_sheet` | FAIL | `test_sales_advisor_aligned` |
| `test_calculator_matches_topology_engine` | FAIL | `test_sales_advisor_performance` |
| `test_han_baicheng_insurance_add_600` | FAIL | `test_sales_advisor_performance` |
| `test_pipeline_w_ai_zero_diff_after_overlay` | FAIL | `test_sales_advisor_performance` |
| `test_same_column_reads_share_cache` | FAIL | `test_workbook_loader_cache` |

上述均为金标准 parity / Hub 公式 / 绩效整理表回归，不涉及 `onboard-month`、`--month` 或 `month_config` 注入。

### 非 verbose 输出尾部特征

```
..............F....E.................................  # 早期 F/E
...F...                                               # hub_formula_engine
..FF...                                               # hub_parity_bundle
.....................................................  # 后续大量通过
```

---

## 验收清单

| 项 | 结果 | 证据 |
|----|------|------|
| 设计文档移除旧手工五步 | ✅ | `新月接入与Hub拓扑.md` §2 CLI 流程 |
| 文档含 inherit vs extract 示例 | ✅ | 同上 §2 表格与 bash 示例 |
| 保留 uploads/topology、骨架、故障、no-bootstrap | ✅ | §1、§3、§4、§6 |
| 更新日志 Unreleased 已写 | ✅ | `更新日志.md` |
| `test_multi_month_cli` 全通过 | ✅ | 12 tests OK |
| 全量 `discover` 全绿 OK | ⚠️ | Ran 324：15 failures + 1 error；见 `_stage5_test_final.txt` |

---

## 最高规则合规

- ✅ 文档明确 `--inherit-topology` 仅继承 topology JSON，不拷贝金标准数值
- ✅ 未为通过验收而从金标准 bootstrap 系统产出
