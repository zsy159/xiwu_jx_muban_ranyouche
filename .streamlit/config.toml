# 观察台开发：试算等长任务在后台线程运行；保存文件不再自动重跑页面，避免中断试算。
# https://docs.streamlit.io/develop/concepts/configuration/config.toml
#
# 请用项目根执行：.venv/bin/python run_console.py

[server]
runOnSave = false
fileWatcherType = "auto"

[runner]
fastReruns = true
